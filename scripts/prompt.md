Analyse the traffic profiles of Havoc framework. Use the files locally which already include an analysis of the code base with  
  .md file extension for reference. Check the code base as well for how and what traffic protocols Havoc currently, accepts.      
  Plan a way to add DNS as a traffic protocol for havoc. Consider all of the hardening and obfuscation implementation so far to   
  keep the new traffic method secure. Functionality of the demon agents must be kept in tact for all other communication          
  methods. Consider the sleep obfuscation when designing the DNS communication method. Write the documentation, design,           
  architecture and implementation in a file locally which can referenced later. Include changes to the client UI + teamserver +   
  demon agent and any other function/model. Use agents to separate the work, anaylsis, design and implementation. Use the red     
  team developer + code qa + code teste agents and more if necessary. Wait for approval on critical actions.